const puppeteer = require("puppeteer"); (async () => { const browser = await puppeteer.launch({headless: "new"}); const page = await browser.newPage(); page.on("pageerror", err => console.log("PAGE ERROR:", err.toString())); page.on("console", msg => console.log("CONSOLE LOG:", msg.type(), msg.text())); await page.goto("http://localhost:4173/"); await new Promise(r => setTimeout(r, 5000)); await browser.close(); })();
